
export function triangular(num) {
    let forTriangular =0;

    for (let index = 1; index <= num; index++) {
        
        forTriangular = (index*(index+1))/(2)
        
    }
    return forTriangular;
}
